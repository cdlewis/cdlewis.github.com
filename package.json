{
  "name": "chris-blag",
  "version": "1.0.0",
  "description": "Instructions for when I next forget how to do things.",
  "main": "",
  "scripts": {
    "start": "./start_dev.sh",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "rbenv install && gem install bundler && rbenv rehash && bundle install",
    "precommit": "lint-staged"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cdlewis/cdlewis.github.com.git"
  },
  "author": "Chris Lewis",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cdlewis/cdlewis.github.com/issues"
  },
  "homepage": "http://chrislewis.com.au",
  "dependencies": {
    "husky": "^4.2.0",
    "lint-staged": "^10.0.0",
    "node-sass": "^4.13.1",
    "prettier": "^1.19.1"
  },
  "lint-staged": {
    "*.{js,json,scss}": [
      "prettier --write",
      "git add"
    ]
  }
}
