{% if forloop.index == null or forloop.first == true %}
  <p id="top_time">{{page.date | date: "%-d %B %Y" }}</p>
{% endif %}
<div class="post">
  <h2>
    <a href="{{ page.url }}">{{ page.title }}</a>
  </h2>
  {{page.excerpt}}
  <a href="{{ page.url }}">Continue reading &#187;</a>
</div>
