<header class="begin">
  {% if forloop.index == null or forloop.first == true %}
    <div id="top_time">{{page.date | date: "%-d %B %Y" }}</div>
  {% endif %}
</header>
<article class="post">
  <h2 class="entry-title">
    <a href="{{ page.url }}">{{ page.title }}</a>
  </h2>
  {{page.content}}
</article>
